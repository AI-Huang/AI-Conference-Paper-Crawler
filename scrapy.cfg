# Automatically created by: scrapy startproject
#
# For more information about the [deploy] section see:
# https://scrapyd.readthedocs.io/en/latest/deploy.html

[settings]
default = ai_conference_paper_crawler.settings

# Code-Data separation: keep Scrapy's project data dir (.scrapy) out of the
# source tree. `data/` is a symlink to $HOME/Data/<ProjectName>, so this lands
# under the external runtime-data directory. Path is relative to the working
# directory; run.sh always cd's to the repo root before invoking scrapy.
[datadir]
default = data/.scrapy

[deploy]
#url = http://localhost:6800/
project = ai_conference_paper_crawler
